"use client";

/**
 * Home Back navigation is handled by the beforeInteractive script in root layout
 * (see HOME_BACK_NAV_SCRIPT). This component remains for future navigation hooks.
 */
export default function NavigationGuard() {
  return null;
}

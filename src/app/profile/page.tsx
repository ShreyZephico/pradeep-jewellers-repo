import ProfileDetailsClient from "@/components/profile/ProfileDetailsClient";

export const metadata = {
  title: "Personal details | Pradeep Jewellers",
  description: "Update your account name, email, phone, and password.",
};

export default function ProfilePage() {
  return <ProfileDetailsClient />;
}

import ProfileAddressesClient from "@/components/profile/ProfileAddressesClient";

export const metadata = {
  title: "Addresses | Pradeep Jewellers",
  description: "Manage your saved delivery addresses.",
};

export default function ProfileAddressesPage() {
  return <ProfileAddressesClient />;
}
